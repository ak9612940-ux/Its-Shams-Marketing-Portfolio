"use client";

import React, { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { Menu, X, Rocket, Sparkles, ChevronRight } from "lucide-react";
import { cn } from "@/lib/utils";

export default function Portfolio() {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  return (
    <div className="min-h-screen bg-white text-slate-900 selection:bg-blue-100">
      {/* --- Navigation --- */}
      <nav className="fixed w-full z-50 bg-white/70 backdrop-blur-xl border-b border-slate-100">
        <div className="max-w-7xl mx-auto px-6 h-20 flex justify-between items-center">
          <motion.div 
            initial={{ opacity: 0, x: -20 }}
            animate={{ opacity: 1, x: 0 }}
            className="flex items-center gap-2 font-bold text-2xl tracking-tighter"
          >
            <div className="bg-blue-600 p-1.5 rounded-lg">
              <Rocket className="text-white size-5" />
            </div>
            <span>Shams<span className="text-blue-600">.</span></span>
          </motion.div>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center gap-8 text-sm font-medium">
            {["Services", "Work", "Process"].map((item) => (
              <a key={item} href={`#${item.toLowerCase()}`} className="text-slate-600 hover:text-blue-600 transition-colors">
                {item}
              </a>
            ))}
            <button className="px-5 py-2.5 bg-slate-900 text-white rounded-full hover:bg-blue-600 transition-all duration-300 shadow-lg shadow-blue-900/10">
              Start a Project
            </button>
          </div>

          {/* Mobile Toggle */}
          <button className="md:hidden p-2" onClick={() => setIsMenuOpen(!isMenuOpen)}>
            {isMenuOpen ? <X /> : <Menu />}
          </button>
        </div>
      </nav>

      {/* --- Hero Section --- */}
      <main className="relative pt-44 pb-24 px-6">
        <div className="max-w-6xl mx-auto text-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-50 border border-blue-100 text-blue-700 text-xs font-bold uppercase tracking-wider mb-8"
          >
            <Sparkles className="size-3" />
            <span>Digital Content Creator & Developer</span>
          </motion.div>
          
          <motion.h1 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.1 }}
            className="text-6xl md:text-8xl font-extrabold tracking-tight mb-8 leading-[0.9]"
          >
            Elevating Brands with <br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-indigo-500 to-purple-600">
              Digital Precision.
            </span>
          </motion.h1>

          <motion.p 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.2 }}
            className="text-lg md:text-xl text-slate-500 max-w-2xl mx-auto mb-12 leading-relaxed"
          >
            I build high-performance web experiences and SEO-driven content strategies 
            that turn digital presence into measurable growth.
          </motion.p>

          <motion.div 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.3 }}
            className="flex flex-col sm:flex-row items-center justify-center gap-4"
          >
            <button className="w-full sm:w-auto px-8 py-4 bg-blue-600 text-white rounded-2xl font-bold hover:bg-blue-700 hover:-translate-y-1 transition-all flex items-center justify-center gap-2 group">
              View Case Studies
              <ChevronRight className="size-4 group-hover:translate-x-1 transition-transform" />
            </button>
            <button className="w-full sm:w-auto px-8 py-4 bg-slate-50 text-slate-900 border border-slate-200 rounded-2xl font-bold hover:bg-white transition-all">
              The Process
            </button>
          </motion.div>
        </div>

        {/* --- Background Decorative Elements --- */}
        <div className="absolute top-0 left-1/2 -translate-x-1/2 -z-10 w-full max-w-4xl h-[500px] bg-blue-400/5 blur-[120px] rounded-full" />
      </main>

      {/* --- AI Demo Section (Small Integration) --- */}
      <section className="py-20 px-6 bg-slate-50 border-y border-slate-100">
        <div className="max-w-4xl mx-auto bg-white p-8 rounded-3xl border border-slate-200 shadow-sm text-center">
          <h3 className="text-2xl font-bold mb-4">Experience the Edge</h3>
          <p className="text-slate-500 mb-6 italic">
            "Combining Google Gemini AI with Next.js 15 for smarter marketing automation."
          </p>
          <div className="inline-block px-6 py-3 bg-slate-900 text-white rounded-xl text-sm font-medium">
            AI Integration Active
          </div>
        </div>
      </section>
    </div>
  );
}
